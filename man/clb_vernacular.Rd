% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/usage.R
\name{clb_vernacular}
\alias{clb_vernacular}
\title{Vernacular (common) names}
\usage{
clb_vernacular(
  id = NULL,
  dataset = "3LXR",
  q = NULL,
  lang = NULL,
  ...,
  limit = 50L,
  max = limit,
  .raw = FALSE
)
}
\arguments{
\item{id}{Optional taxon id. When supplied, returns the vernacular names of
that taxon; otherwise performs a dataset-wide vernacular search using \code{q}.}

\item{dataset}{Dataset key or alias. Defaults to \code{"3LXR"}.}

\item{q}{Free-text query for the dataset-wide search (ignored when \code{id} is
supplied).}

\item{lang}{Optional ISO language filter (e.g. \code{"eng"}, \code{"deu"}).}

\item{...}{Further query parameters.}

\item{limit}{Page size for the dataset-wide search.}

\item{max}{Maximum rows for the dataset-wide search.}

\item{.raw}{Return the raw parsed JSON instead of a tibble?}
}
\value{
A \link[tibble:tibble]{tibble} of vernacular names with columns such as
\code{name}, \code{latin}, \code{language} and \code{taxonID}.
}
\description{
Looks up vernacular names either for a single taxon (when \code{id} is given) or
across a dataset by free-text query.
}
\examples{
\dontrun{
clb_vernacular(id = "4CGXP", dataset = "3LR")
clb_vernacular(q = "lion", lang = "eng")
}
}
